import styled from "styled-components";

export const HomeWrapper = styled.div`
  > .content {
    width: 1034px;
    margin: 0 auto;
  }

  .house {
    display: flex;
    flex-wrap: wrap;
    margin: 0 -8px;
  }
`